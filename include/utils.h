#ifndef UTILS_H
#define UTILS_H

#include <string>

using namespace std;

class utils
{
public:

	static string toString( double );
	static string toString( int );
	static string toString( uint );

	utils();
	~utils();

	/* data */
};

#endif